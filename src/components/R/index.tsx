import * as React from 'react';
import { RPage } from './styled';

interface ReadProps {
}

const Read: React.FunctionComponent<ReadProps> = (props) => {
  return <RPage>Read...</RPage>;
};

export default Read;